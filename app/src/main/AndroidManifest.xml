<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.febrian.musicplayerapp">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_baseline_music_note_24_blue"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_baseline_music_note_24_blue"
        android:supportsRtl="true"
        android:theme="@style/Theme.MusicPlayerApp">
        <activity
            android:name=".ui.activity.MusicPlayerActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>